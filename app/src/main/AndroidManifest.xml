<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RentHouse">
        <!--hedhoum zouz badalthom "authentication.LoginActivity" kent "splashscreen.SplashScreenActivity" w loutaneya bel3aks-->
        <activity android:name="com.example.renthouse.authentication.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name="com.example.renthouse.payment.PaymentActivity" />
        <!--hedhi loutaneya-->
        <activity
            android:name="com.example.renthouse.splashscreen.SplashScreenActivity"
            android:theme="@style/Theme.RentHouseNoActionBar">

        </activity>
        <activity android:name="com.example.renthouse.homepage.HomePage" />
        <activity android:name="com.example.renthouse.BottomNavigationBar.BootomNavBarMain" />
        <activity android:name="com.example.renthouse.renthouse.AddHouseActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>